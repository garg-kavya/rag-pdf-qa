# =============================================================================
# Default Configuration — RAG PDF Q&A
# =============================================================================
# Base configuration values. Environment variables take precedence.
# =============================================================================

# app:
#   name: "RAG PDF Q&A"
#   version: "0.1.0"
#   debug: false
#   log_level: "INFO"
#   upload_dir: "./uploads"
#   max_upload_size_mb: 50

# openai:
#   embedding_model: "text-embedding-3-small"
#   embedding_dimensions: 1536
#   embedding_batch_size: 100
#   llm_model: "gpt-4o"
#   llm_temperature: 0.1
#   llm_max_tokens: 1024

# chunking:
#   chunk_size_tokens: 512
#   chunk_overlap_tokens: 64
#   split_separators:
#     - "\n\n"
#     - "\n"
#     - ". "
#     - " "

# retrieval:
#   vector_store_type: "faiss"
#   top_k: 5
#   top_k_candidates: 10
#   similarity_threshold: 0.70
#   mmr_diversity_factor: 0.3

# reranker:
#   backend: "none"           # "none" | "cross_encoder" | "cohere"
#   cohere_api_key: null
#   cross_encoder_model: "cross-encoder/ms-marco-MiniLM-L-6-v2"

# cache:
#   backend: "memory"
#   max_size: 1000
#   embedding_cache_ttl_seconds: 86400
#   response_cache_ttl_seconds: 60

# memory:
#   token_budget: 1024
#   compression_threshold: 10
#   compression_turns: 5

# session:
#   ttl_minutes: 60
#   max_conversation_turns: 10
#   cleanup_interval_seconds: 300

# server:
#   host: "0.0.0.0"
#   port: 8000
#   workers: 1
#   cors_origins:
#     - "*"
